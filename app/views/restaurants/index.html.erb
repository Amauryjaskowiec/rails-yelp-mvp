<h1>
Here are all the restaurants <% @restaurants.each do |restaurant| %>
  <h2><%= restaurant.name %></h2>
  <p><%= restaurant.address %></p>
  <p><%= restaurant.phone_number %></p>
<%= link_to "See the reviews", restaurant_path(restaurant), {class: "btn btn-primary"} %>
<% end %>
<%= link_to "Reference a new restaurant", new_restaurant_path, {class: "btn btn-primary"} %>

</h1>
